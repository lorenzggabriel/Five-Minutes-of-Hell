package tile;

// Importing the BufferedImage class from the java.awt.image package
import java.awt.image.BufferedImage;

// Definition of the Tile class
public class Tile {

    // Public field 'image' of type BufferedImage to store the tile image
    public BufferedImage image;

    // Public field 'collision' of type boolean to represent whether the tile has
    // collision or not, initialized to false
    public boolean collision = false;

}
